# Інструкція з деплою на Render.com (Free Plan)

Ця інструкція допоможе вам опублікувати проект на Render.com безкоштовно.

## 📋 Передумови

1. Аккаунт на [Render.com](https://render.com) (безкоштовна реєстрація)
2. Git репозиторій (GitHub, GitLab або Bitbucket)
3. Проект готовий до деплою

## 🚀 Крок 1: Підготовка Git репозиторію

1. Створіть Git репозиторій (якщо ще не створено):
   ```bash
   git init
   git add .
   git commit -m "Ready for Render deployment"
   git remote add origin <ваш-репозиторій-url>
   git push -u origin main
   ```

2. Переконайтеся, що всі файли закомічені:
   ```bash
   git status
   ```

## 🗄️ Крок 2: Створення бази даних PostgreSQL на Render

1. Увійдіть на [dashboard.render.com](https://dashboard.render.com)
2. Натисніть **"New +"** → **"PostgreSQL"**
3. Налаштування:
   - **Name**: `training-recording-db`
   - **Database**: `training_recording`
   - **User**: `training_user`
   - **Plan**: **Free**
   - **Region**: виберіть найближчий до вас
4. Натисніть **"Create Database"**
5. **ВАЖЛИВО**: Зачекайте поки база даних створиться (1-2 хвилини)

## 🌐 Крок 3: Створення Web Service на Render

### Варіант А: Автоматичний деплой через Blueprint (рекомендовано)

1. У вашому репозиторії є файл `render.yaml` з налаштуваннями
2. На Render Dashboard натисніть **"New +"** → **"Blueprint"**
3. Підключіть ваш Git репозиторій
4. Render автоматично визначить `render.yaml` і створить всі сервіси (базу даних та Web Service)
5. Натисніть **"Apply"**

### Варіант Б: Ручне створення Web Service

Якщо ви не використовуєте Blueprint:

1. Натисніть **"New +"** → **"Web Service"**
2. Підключіть ваш Git репозиторій
3. Налаштування:
   - **Name**: `training-recording-backend`
   - **Environment**: **Node**
   - **Region**: той самий, що й база даних
   - **Branch**: `main` (або ваша гілка)
   - **Root Directory**: залиште порожнім (проект в корені)
   - **Build Command**: `cd backend && npm install && npx prisma generate && npx prisma migrate deploy`
   - **Start Command**: `cd backend && npm start`
   - **Plan**: **Free**

4. **Environment Variables** (додайте вручну):
   - `NODE_ENV` = `production`
   - `DATABASE_URL` = (скопіюйте Internal Database URL з бази даних, яку створили)
   - `DIRECT_URL` = (той самий, що й DATABASE_URL)
   - `JWT_SECRET` = (натисніть "Generate" для автоматичного створення)
   - `CORS_ORIGIN` = `*` (або ваш домен, якщо знаєте)

5. Натисніть **"Create Web Service"**

## ⚙️ Крок 4: Очікування деплою

1. Render автоматично почне деплой після створення сервісу
2. Спостерігайте за логами в Render Dashboard
3. Деплой займає 5-10 хвилин
4. Після успішного деплою скопіюйте URL вашого сервісу (наприклад: `https://training-recording-backend.onrender.com`)

## 🌱 Крок 5: Ініціалізація бази даних

1. Після першого деплою база даних буде автоматично міграцій через `prisma migrate deploy`
2. **Адміністратор створюється автоматично** при першому запуску сервера (якщо його ще немає)
   - Email: `admin@test.local`
   - Password: `admin123`
   - Role: SystemAdmin
   
   **Примітка**: Якщо вам потрібно створити адміністратора вручну (наприклад, якщо автоматичне створення не спрацювало):
   - Відкрийте **Shell** в Render Dashboard для вашого Web Service (потрібен платний план)
   - Або виконайте: `cd backend && npm run prisma:seed`

## ✅ Крок 6: Перевірка працездатності

1. Відкрийте ваш Render URL: `https://training-recording-backend.onrender.com`
2. Перевірте health check: `https://training-recording-backend.onrender.com/api/health`
3. Перевірте базу даних: `https://training-recording-backend.onrender.com/api/health/db`
4. Перевірте API endpoint: `https://training-recording-backend.onrender.com/api`

Всі запити повинні повертати JSON з `success: true`.

## 📝 Важливі примітки для Free Plan

### Обмеження Free Plan:
- **Sleep Mode**: Сервіс "засинає" після 15 хвилин неактивності
- **Cold Start**: Перший запит після "сну" може займати 30-60 секунд
- **Build Time**: До 15 хвилин на місяць
- **Bandwidth**: 100 GB на місяць

### Рекомендації:
1. Для production використовуйте **Starter Plan** ($7/місяць) - без sleep mode
2. Налаштуйте **Health Check** в Render для швидшого пробудження
3. Використовуйте **Render Cron Jobs** для періодичних завдань (якщо потрібно)

## 🔧 Troubleshooting

### Помилка підключення до бази даних
- Перевірте, що `DATABASE_URL` правильний
- Переконайтеся, що база даних створена і активна
- Перевірте, що використовується **Internal Database URL** (не External)
- Перевірте логи в Render Dashboard

### Помилка при міграції
- Перевірте логи в Render Dashboard
- Переконайтеся, що `prisma migrate deploy` виконується успішно
- Можна виконати вручну через Shell: `cd backend && npx prisma migrate deploy`

### CORS помилки
- Перевірте `CORS_ORIGIN` в Environment Variables
- Для тестування встановіть `*`
- Для production вкажіть конкретний домен

### Статичні файли не завантажуються
- Перевірте, що всі файли в репозиторії
- Переконайтеся, що `server.js` правильно обслуговує статичні файли
- Перевірте логи в Render Dashboard

### Сервіс не стартує
- Перевірте логи в Render Dashboard
- Переконайтеся, що `PORT` змінна доступна (Render автоматично встановлює)
- Перевірте, що всі залежності встановлені (`npm install` виконано успішно)

### Адміністратор не створюється автоматично
- Перевірте логи сервера - там має бути повідомлення про створення адміністратора
- Переконайтеся, що база даних міграцій спочатку (ролі мають бути створені)
- Якщо автоматичне створення не спрацювало, виконайте через Shell: `cd backend && npm run prisma:seed`
- Або створіть адміністратора вручну через реєстрацію та змініть роль через API

## 🔐 Тестові дані (після seed)

Після виконання `npm run prisma:seed`:
- **Email**: `admin@test.local`
- **Password**: `admin123`
- **Role**: SystemAdmin

## 📞 Підтримка

- [Render Documentation](https://render.com/docs)
- [Render Community](https://community.render.com)
- [Render Status](https://status.render.com)

## 🎉 Готово!

Ваш проект тепер доступний на Render.com. URL буде виглядати як:
`https://training-recording-backend.onrender.com`

**Примітка**: На Free Plan перший запит може займати час через sleep mode. Для production рекомендую оновитися до Starter Plan.

---

## 📋 Швидкий чеклист

- [ ] Git репозиторій створено і файли закомічені
- [ ] PostgreSQL база даних створена на Render
- [ ] Web Service створено (через Blueprint або вручну)
- [ ] Environment Variables налаштовані
- [ ] Деплой завершено успішно
- [ ] База даних міграцій (`prisma migrate deploy`)
- [ ] Seed виконано (`npm run prisma:seed`)
- [ ] Health check працює (`/api/health`)
- [ ] Database health check працює (`/api/health/db`)
- [ ] Тестові дані для входу працюють
