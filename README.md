# Training Recording System

Система навчання та запису тренувань для військового персоналу.

## 🚀 Деплой на Render.com

**Детальна інструкція**: Дивіться [RENDER_DEPLOY.md](./RENDER_DEPLOY.md)

### Швидкий огляд:

1. **Створіть PostgreSQL базу даних** на Render (Free Plan)
2. **Створіть Web Service** через Blueprint (використовує `render.yaml`) або вручну
3. **Налаштуйте Environment Variables**:
   - `DATABASE_URL` - з вашої PostgreSQL бази
   - `DIRECT_URL` - той самий, що й DATABASE_URL
   - `JWT_SECRET` - згенеруйте автоматично
   - `CORS_ORIGIN` - `*` для тестування
4. **Деплой автоматично** через Git push

## 📁 Структура проекту

```
├── backend/          # Node.js/Express API
│   ├── src/         # Код додатку
│   ├── prisma/      # База даних схема
│   └── server.js    # Точка входу
├── app/             # Frontend файли
├── css/             # Стилі
├── js/              # JavaScript файли
└── render.yaml      # Конфігурація Render
```

## 🔧 Локальна розробка

```bash
# Встановлення залежностей
cd backend
npm install

# Налаштування бази даних
cp .env.example .env  # Створіть .env файл
# Додайте DATABASE_URL до .env

# Міграція бази даних
npx prisma migrate dev

# Заповнення тестовими даними
npm run prisma:seed

# Запуск сервера
npm start
```

## 🔐 Тестові облікові дані (після seed)

- **Email**: `admin@test.local`
- **Password**: `admin123`
- **Role**: SystemAdmin

## 📝 Технології

- **Backend**: Node.js, Express, Prisma ORM
- **Database**: PostgreSQL
- **Frontend**: Vanilla JavaScript, HTML, CSS

## 📄 Ліцензія

ISC
